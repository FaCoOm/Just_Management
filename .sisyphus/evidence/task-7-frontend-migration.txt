Task 7 frontend migration verification
Date: 2026-05-01

Files modified:
- src/types/database.ts
- src/hooks/use-dashboard-data.ts

Compatibility decision:
- `Reservation` and normalized `ReservationStatus` are now exposed in `src/types/database.ts` from the reservation-core migration contract.
- UI-facing `Guest` remains as a compatibility view model alias (`DashboardGuest`) because existing dashboard, rooms, reservations, and guests pages are still guest-labeled.
- `useDashboardData` derives `guests` from `reservations`; it no longer reads the legacy `guests` table as the authoritative booking source.

Source-read verification:
- Search in `src/hooks/use-dashboard-data.ts` found `supabase.from("reservations").select("*").order("check_in_date")`.
- Search in `src/hooks/use-dashboard-data.ts` found no `supabase.from("guests")` booking query.

LSP diagnostics:
- `src/types/database.ts`: no diagnostics found.
- `src/hooks/use-dashboard-data.ts`: no diagnostics found.

Command: npm run typecheck
Result: PASS
Output:
> shadcn-ui-template@0.0.1 typecheck
> tsc --noEmit

Command: npm run build
Result: PASS
Output summary:
> shadcn-ui-template@0.0.1 build
> tsc -b && vite build

vite v7.3.1 building client environment for production...
2868 modules transformed.
dist/index.html 1.07 kB gzip 0.53 kB
dist/assets/index-CEZfXG-m.css 126.65 kB gzip 19.58 kB
dist/assets/index-BMKBW0V5.js 1,020.49 kB gzip 297.06 kB
Built in 5.86s.

Build warning observed:
- Vite reported some chunks are larger than 500 kB after minification. This is an existing bundle-size warning pattern, not a TypeScript or schema-contract failure.
